<?php
/*
Make two functions (maximum and minimum) that outputs, respectively,
the largest and the lowest number in array.
*/

function maximum($array) {
  return max($array);
}

function minimum($array) {
  return min($array);
}
